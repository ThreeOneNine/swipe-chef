function toggleCategoryButton(category_id) {
	const categoryButton = document.getElementById(category_id);
	categoryButton.classList.toggle('category-button-selected');
}

toggleCategoryButton(<%= @category.id %>)